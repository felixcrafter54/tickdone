# Shell-Skripte müssen LF behalten – im Docker-Image (Linux/busybox) würde ein
# CRLF-Zeilenende die Ausführung/das Sourcing brechen.
*.sh text eol=lf
*.envsh text eol=lf
# nginx-Config/Template ebenfalls LF (wird im Linux-Container verarbeitet).
*.template text eol=lf
*.conf text eol=lf
